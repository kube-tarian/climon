
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: prometheusdeployments.monitoring.climon.com
spec:
  group: monitoring.climon.com
  names:
    kind: PrometheusDeployment
    listKind: PrometheusDeploymentList
    plural: prometheusdeployments
    singular: prometheusdeployment
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: PrometheusDeployment is the Schema for the prometheusdeployments
        API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: PrometheusDeploymentSpec defines the desired state of PrometheusDeployment
          properties:
            last_modifier:
              description: Last modifier of the custom resource
              type: string
            metadata:
              description: Metadata of the prometheusdeployment
              type: string
            name:
              description: 'INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                Important: Run "make" to regenerate code after modifying this file'
              type: string
            prometheus:
              description: Defines the Prometheus chart details
              properties:
                enabled:
                  description: Specifies if the prometheus chart is to be installed.
                    Assumed to be true if not specified
                  type: boolean
                repo:
                  description: Specifies the repo from where prometheus chart is to
                    be used
                  type: string
                version:
                  description: Version of the chart to be installed
                  type: string
              required:
              - enabled
              - repo
              type: object
          required:
          - last_modifier
          - name
          type: object
        status:
          description: PrometheusDeploymentStatus defines the observed state of PrometheusDeployment
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
